package org.example;

public class Teht1 {
    // Tehtävä 1
    public static double calculateDistance(int[] p1, int[] p2) {
        return Math.sqrt(Math.pow((p1[0] - p1[1]), 2) + Math.pow((p2[0] - p2[1]), 2));
    }
}
